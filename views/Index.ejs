<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CureAI Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.4/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <!-- Header -->
  <header class="bg-blue-600 p-4">
    <div class="container mx-auto flex justify-between items-center">
      <!-- Website Name -->
      <h1 class="text-white text-2xl font-semibold">CureAI</h1>
      
      <!-- Buttons (Login/Signup) -->
      
      <div class="flex items-center">
        <% if (userName) { %>
          <div class="flex items-center space-x-6">
            <p class="text-white text-lg font-semibold">Welcome, <%= userName %></p>
            <p><a href="/logout" class="text-white text-base hover:underline">LogOut</a></p>
          </div>
        <% } else { %>
          <a href="/login" class="text-white mr-4">Login</a>
          <a href="/signup" class="text-white">Sign Up</a>
        <% } %>
      </div>
    </div>
  </header>
    </div>
  </header>

  <!-- Main Content -->
  <main class="container mx-auto mt-10">
    <div class="flex justify-center items-center">
      <!-- Stylish Upload Button -->
      <div class="text-center">
        <h2 class="text-xl mb-4">Upload Your Image</h2>
        <form action="/upload" method="POST" enctype="multipart/form-data">
          <input type="file" name="image" class="border-2 p-2 rounded-lg cursor-pointer bg-blue-500 text-white hover:bg-blue-600 transition-all">
          <button type="submit" class="mt-4 px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700">Upload</button>
        </form>
      </div>
    </div>
  </main>

</body>
</html>
